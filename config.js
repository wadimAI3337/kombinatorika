/* ============================================================
   Настройки облачных сохранений.

   Впиши сюда два значения из своего проекта Supabase:
   Supabase → Project Settings → API
     • Project URL      →  SUPABASE_URL
     • anon public key  →  SUPABASE_ANON_KEY

   Эти два значения не секретные: ключ anon специально сделан
   публичным, данные защищает RLS (см. schema.sql). Их можно
   спокойно коммитить в открытый репозиторий.

   Пока значения не заполнены, сайт работает как раньше —
   прогресс просто лежит в этом браузере, кнопки входа нет.
   ============================================================ */

window.KOMBI_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
